-- On a string switch between " and '. On an object { } switches between single
-- and multiples lines. On variable switch between camelCase, PascalCase etc.
-- Boolean switches between true/false.
return {
    "CKolkey/ts-node-action",
    opts = {},
    cmd = {
      "NodeAction",
    },
}
